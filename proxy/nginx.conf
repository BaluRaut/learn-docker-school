# The proxy's one job (lesson 06): forward everything to the service
# named "web" — a NAME, not an IP. Docker's network DNS resolves it,
# exactly like Kubernetes Services do later in the k8s course.
events {}
http {
  server {
    listen 80;
    location / {
      proxy_pass http://web:3000;
    }
  }
}
